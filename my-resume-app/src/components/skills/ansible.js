import React from 'react';

const Ansible = () => {
    return (
        <div>
            <h2>Ansible Details</h2>
            <p>Details about Ansible...</p>
        </div>
    );
};

export default Ansible;
